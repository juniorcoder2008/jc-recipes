import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.recipe.juniorcoder.app',
  appName: 'jc-recipe',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
